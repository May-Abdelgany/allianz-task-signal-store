import { StorageState } from '../../models/storagestate';

export const initialState: StorageState = {
  boxes: [],
  selectedBox: null,
  saltos: null,
  saltoOptionSelected: null,
  totalSaltos: 0,
};
